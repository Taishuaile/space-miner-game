# 0->1 級文明產品規格書

## 1. 範圍

本版完整主軸為「從 0 級文明發展到 1 級文明」。

- 本版只做 `0 -> 1` 流程
- `1 級後宇宙探索` 不在本版實作範圍（僅顯示入口）
- 沿用既有經濟與數值核心，不重寫 BigNum / 商店公式

## 2. 破關條件（已調高）

你提的方向正確：要避免玩家還沒碰到後段內容就破關。  
本版採用「最後道具達成條件」作為核心門檻。

### 2.1 主條件（必要）

1. `akashicPrinter.count >= 1`

說明：

- `akashicPrinter` 是最後一個道具
- 只要要求它至少購買 1 次，就能保證玩家確實走完整條道具線

### 2.2 建議附加條件（可選）

1. `文明能量（score） >= 5e13`

用途：

- 避免「剛買到最後道具就瞬間破關」太突兀
- 讓玩家在終局多停留一小段，感受 1 級文明完成感

### 2.3 數值依據

以目前平衡資料推估：

- 最後道具 `akashicPrinter` 基礎成本約 `2.1e13`
- 商店解鎖門檻為 `baseCost * 0.5`，約 `1.05e13`

因此只要把破關綁在 `akashicPrinter.count >= 1`，就已明顯高於你原本舊門檻（`1e12`）很多，不會太早破關。

## 3. 核心迴圈（不改設計）

1. 點擊地球取得文明能量
2. 購買建設提高被動產能
3. 購買文明突破（特殊強化）提升倍率
4. 使用自動購買加速成長
5. 購買到最後道具並達成破關條件

## 4. UI 需求

### 4.1 必要

1. 分數區旁新增文明狀態面板
- 文明等級（0 級 / 1 級）
- 破關條件追蹤
- 進度顯示

2. 全域文案改名
- `星能` -> `文明能量`
- `每秒星能` -> `每秒文明能量`

3. 中央主星球 icon 改為地球
- `🪐` -> `🌍`

### 4.2 可選

1. 破關動畫/彈窗
2. `探索宇宙（即將開放）` 按鈕（disabled）

## 5. 資料模型需求

`game` 新增：

- `civilizationLevel: 0`
- `civilizationCleared: false`
- `civilizationClearRule: { requireLastItem: true, minAkashicCount: 1, optionalScoreGate: 5e13 }`

## 6. 必改邏輯

1. 破關判定函式
- 新增 `isCivilizationCleared()`
- 檢查 `game.upgrades.akashicPrinter.count >= 1`
- 若啟用附加門檻，再檢查 `score >= 5e13`

2. 一次性破關觸發
- 新增 `onCivilizationCleared()`
- 只觸發一次，避免重複彈窗/重複事件

3. 存檔與讀檔
- `saveGame()` 寫入文明狀態欄位
- `loadGame()` 對舊存檔採預設值（相容）

4. UI 刷新
- 初始化時渲染文明面板
- 每次資源更新/購買後刷新條件顯示

## 7. 本版不改項目

1. BigNum 實作
2. 價格公式（`1.15^count`）
3. 自動購買評估公式
4. 特殊強化倍率機制
5. 1 級後新玩法主循環

## 8. 驗收標準

1. 玩家未買到最後道具前不可破關
2. 購買到 `akashicPrinter` 後可破關（有附加門檻時須同時達成）
3. 破關狀態重整後可保留
4. 現有玩法無回歸
- 點擊
- 商店購買與解鎖
- 特殊強化
- 自動購買
- 單位切換

